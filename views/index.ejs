<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create User</title>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="min-h-screen w-full bg-zinc-900 p-5 sm:p-10 text-white">
      
      <div class="flex justify-center mb-10">
        <a class="text-blue-700 text-lg sm:text-xl hover:underline" href="/read">Read Users</a>
      </div>

      <div class="max-w-3xl mx-auto">
        <h1 class="text-2xl sm:text-3xl p-5 tracking-tighter text-center mb-4">Create User</h1>

        <form
          action="/create"
          method="post"
          enctype="multipart/form-data"
          class="flex flex-col space-y-5 w-full md:w-2/3 lg:w-1/2 mx-auto px-4 sm:px-0"
        >
          <input
            class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent"
            type="text"
            name="name"
            placeholder="Name"
            required
          />

          <input
            class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent"
            type="email"
            name="email"
            placeholder="Email"
            required
          />
          <div>
            <label class="block text-sm mb-1 text-zinc-300">Upload Image</label>
            <input
              type="file"
              name="imageFile"
              accept="image/*"
              class="w-full text-sm border-2 border-zinc-800 rounded-lg bg-transparent p-2"
            />
          </div>

     
          <div>
            <label class="block text-sm mb-1 text-zinc-300">Or Enter Image URL</label>
            <input
              class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent w-full"
              type="text"
              name="imageUrl"
              placeholder="Image URL"
            />
          </div>

          <input
            class="px-5 py-2 bg-blue-500 text-black font-semibold rounded-lg cursor-pointer hover:bg-blue-400 transition"
            type="submit"
            value="Create User"
          />
        </form>
      </div>
    </div>
  </body>
</html>
