<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update User</title>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 text-white">
    <div class="min-h-screen w-full p-5 sm:p-10 flex flex-col items-center">

      <div class="w-full flex justify-center mb-6">
        <a class="text-blue-500 text-base sm:text-lg hover:underline" href="/read">← Read Users</a>
      </div>
      <div class="w-full max-w-xl bg-zinc-800 p-6 rounded-xl shadow-lg">
        <h1 class="text-2xl sm:text-3xl font-semibold tracking-tight text-center mb-8">Update User</h1>

        <form
          action="/update/<%= user._id %>"
          method="POST"
          class="flex flex-col gap-5"
        >
          
          <input
            type="text"
            name="name"
            placeholder="Name"
            value="<%= user.name %>"
            required
            class="px-5 py-2 border border-zinc-700 rounded-lg bg-zinc-900 text-white placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500 transition"
          />
          <input
            type="email"
            name="email"
            placeholder="Email"
            value="<%= user.email %>"
            required
            class="px-5 py-2 border border-zinc-700 rounded-lg bg-zinc-900 text-white placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500 transition"
          />
          <input
            type="text"
            name="image"
            placeholder="Image URL"
            value="<%= user.image %>"
            class="px-5 py-2 border border-zinc-700 rounded-lg bg-zinc-900 text-white placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500 transition"
          />
          <input
            type="submit"
            value="Update User"
            class="px-5 py-2 bg-yellow-500 hover:bg-yellow-400 text-black font-semibold rounded-lg cursor-pointer transition"
          />
        </form>
      </div>
    </div>
  </body>
</html>
